<%= form_for(@fieldtype) do |f| %>
  <% if @fieldtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fieldtype.errors.count, "error") %> prohibited this fieldtype from being saved:</h2>

      <ul>
      <% @fieldtype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
